project_name: runner-guard

before:
  hooks:
    - go mod tidy
    - go vet ./...
    - go test ./...

builds:
  - main: ./cmd/runner-guard
    binary: runner-guard
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.ShortCommit}}
      - -X main.date={{.Date}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "^chore:"

brews:
  - repository:
      owner: Vigilant-LLC
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    homepage: "https://github.com/Vigilant-LLC/runner-guard"
    description: "CI/CD source-to-sink vulnerability scanner for GitHub Actions"
    license: "Apache-2.0"
    install: |
      bin.install "runner-guard"
    test: |
      system "#{bin}/runner-guard", "version"

release:
  github:
    owner: Vigilant-LLC
    name: runner-guard
  draft: false
  prerelease: auto
  name_template: "v{{.Version}}"
